{# empty Twig template #}

<p>Liste des plats</p>


{% for plat in resultat %}

  <div class="product-card">
  <div class="product-card-thumbnail">
   {{ plat['image']|raw }}
  </div>
  <h2 class="product-card-title">{{ plat['nom'] }}</h2>
  <span class="product-card-desc">  {{ plat['description'] }}</span>
  <span class="product-card-price">  {{ plat['prix'] }} €</span>
  <div class="product-card-colors">
    <button type="button" class="tiny success button" style="font-size:2rem;"><b>+</b></button>
    <button type="button" class="tiny alert button" style="font-size:2rem;"><b>-</b></button>
  </div>
</div>

{% endfor %}
